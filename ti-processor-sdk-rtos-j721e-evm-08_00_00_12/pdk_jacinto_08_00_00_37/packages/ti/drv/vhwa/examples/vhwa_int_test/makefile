#
# This file is the makefile for building VHWA LDC test app for TI RTOS
#
ifeq ($(RULES_MAKE), )
include $(PDK_INSTALL_PATH)/ti/build/Rules.make
else
include $(RULES_MAKE)
endif

APP_NAME = vhwa_int_testapp

include makefile.mk
